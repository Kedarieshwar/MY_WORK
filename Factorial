# This is program to find the factorial of the given number

def fact(n):
    factorial = 1
    for i in range(1,n+1):
        factorial = factorial * i
    return  print("The factorial of the given input number is:",factorial)
n=int(input("Enter a number to find the factorial:"))
fact(n)